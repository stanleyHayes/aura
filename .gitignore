# Go
/bin/
*.test
*.out
coverage.*

# Env / secrets
.env
.env.*
!.env.example

# Node / web / mobile
node_modules/
.next/
dist/
.expo/
*.tsbuildinfo

# Local Postgres test cluster
/.pgdata/
/.tmp/

# OS / editor
.DS_Store
.idea/
.vscode/

# Turborepo / build caches
.turbo/
**/.turbo/
*.tsbuildinfo

# Local dev credentials (never commit)
credentials.txt

# Local production env reference (real secrets) — never commit
production.env

# Shareable user guide (embeds demo logins) — keep out of version control
USER_GUIDE.html
USER_GUIDE.pdf
